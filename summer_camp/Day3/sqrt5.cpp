#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    freopen("sqrt5.in", "r", stdin);
    freopen("sqrt5.out", "w", stdout);
    long double n = 0;
    cin >> n;

    cout << pow(n, 0.2) << endl;

    return 0;
}